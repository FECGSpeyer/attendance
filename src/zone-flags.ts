/**
 * Zone.js flags to improve performance
 * https://angular.io/guide/zone
 */
// eslint-disable-next-line no-underscore-dangle
(window as any).__Zone_disable_customElements = true;

// Disable patching of high-frequency events that don't need change detection
(window as any).__zone_symbol__UNPATCHED_EVENTS = ['scroll', 'mousemove', 'touchmove', 'wheel', 'pointermove'];
