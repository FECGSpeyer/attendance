/**
 * Attendix Testing Utilities
 *
 * Central export for all testing utilities, mocks, and factories.
 *
 * Usage:
 * import { createPlayer, createDbServiceMock, setupComponentTest } from 'src/testing';
 */

// Factories for creating test data
export * from './factories';

// Mocks for external dependencies
export * from './mocks';

// Test helpers for components and services
export * from './helpers';
